database:
  host: 127.0.0.1
  port: 5432
  user: postgres
  password: postgres
  database_name: ltcats_test_db
  url: "postgresql+asyncpg://postgres:postgres@localhost/notamem" # local
  # url: "postgresql+asyncpg://postgres:postgres@localhost/notamem_db" # docker
  echo: true
keydb:
  host: 127.0.0.1
  port: 6380
  db: 0
  url: "keydb://localhost:6380/0"
jwt:
  secret: "your-secret-key"
  token_secret: "super-secret"
